[project]
name = "ramblefix"
version = "0.1.0"
description = "Local mixed-language voice capture for builders"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["ramblefix"]

[tool.setuptools.package-dir]
"" = "src"
